###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = ghostty
$fileManager = $terminal -e yazi
$menu = wofi
$browser = qutebrowser
$music = youtube-music --enable-features=UseOzonePlatform --ozone-platform=wayland

$scriptfolder=$HOME/scripts

################
### IMPORTS  ###
################

source = ~/.config/hypr/exec.conf
source = ~/.config/hypr/decor.conf
source = ~/.config/hypr/binds.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=eDP-1,preferred,auto,1.33
monitor=HDMI-A-1,preferred,auto-center-left,1.6

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

# enable nvidia card
env = LIBVA_DRIVER_NAME,nvidia
env = __GLX_VENDOR_LIBRARY_NAME,nvidia

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = TERM,alacritty

###################
### PERMISSIONS ###
###################

# See https://wiki.hyprland.org/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow



general {
  layout = scrolling
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

scrolling {
 # See https://wiki.hypr.land/Configuring/Scrolling-Layout/
 focus_fit_method = 1
 column_width = 0.6
 explicit_column_widths = 0.6, 0.95
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}

binds {
    movefocus_cycles_fullscreen = true
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us,sk,us
    kb_variant = colemak_dh,qwerty,
    kb_model =
    kb_options = caps:backspace,grp:win_space_toggle,grp:rctrl_switch
    kb_rules =

    follow_mouse = 1
    numlock_by_default = true
    resolve_binds_by_sym = true

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
    }
}

# https://wiki.hypr.land/Configuring/Keywords#per-device-input-configs
device {
  name = idank-sweeq-keyboard
  kb_layout = us,sk
  kb_variant = ,qwerty
}
device {
    name = elan1200:00-04f3:30f7-touchpad
    enabled = true
}

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

workspace = 1, defaultName:browser, on-created-empty:$browser, monitor:1, persistent:true
workspace = 2, defaultName:code, on-created-empty:$scriptfolder/zellij-hook, monitor:0, persistent:true
workspace = 3, defaultName:editor, monitor:1, persistent:true
workspace = 4, defaultName:text, monitor:0, persistent:true
workspace = 5, defaultName:misc, monitor:0, persistent:true

workspace = special:music, monitor:0, on-created-empty:$music

windowrule = workspace 3, match:class chromium
windowrule = workspace 3, match:class org.pwmt.zathura

windowrule = workspace 4, match:class et
windowrule = workspace 4, match:class wps
windowrule = workspace 4, match:class wpp

windowrule = workspace 5, match:class KeePassXC
windowrule = workspace 5, match:class calibre-gui

# Ignore maximize requests from apps.
windowrule = suppress_event maximize, suppress_event activate, suppress_event activatefocus, match:class .*

# Make terminals with this keyword floating
windowrule = match:initial_class ^custom\.float$, float on, center on, size (monitor_w*0.7)  (monitor_h*0.7)
windowrule = match:initial_class ^custom\.pick$, float on, move (monitor_w*0.35) (monitor_h*0.7), size (monitor_w*0.3) (monitor_h*0.2)
# Other floating
windowrule = match:initial_title ^Open Files$, float on

##############################
###        PLUGINS         ###
##############################
plugin {
    virtual-desktops {
    }
}


##############################
###         MISC           ###
##############################

# disable scaling to prevent pixelation
xwayland {
    force_zero_scaling = true
}
